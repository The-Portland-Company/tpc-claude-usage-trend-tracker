name: ClaudeUsageTrendTracker

options:
  bundleIdPrefix: com.theportlandcompany

configs:
  Debug: debug
  Release: release
  Release-AppStore: release

settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    CURRENT_PROJECT_VERSION: "4"
    MARKETING_VERSION: "1.0"

targets:
  ClaudeUsageTrendTracker:
    type: application
    platform: macOS
    sources:
      - path: Sources
        excludes:
          - "Info.plist"
          - "ClaudeUsageTrendTracker.entitlements"
          - "ClaudeUsageTrendTracker-AppStore.entitlements"
    settings:
      base:
        PRODUCT_NAME: "Claude Usage Trend Tracker"
        # PRODUCT_NAME has a space; pin the Swift module name so tests can
        # `@testable import ClaudeUsageTrendTracker` rather than `Claude_Usage_Trend_Tracker`.
        PRODUCT_MODULE_NAME: ClaudeUsageTrendTracker
        # Bundle id is locked in App Store Connect (builds already uploaded), so it keeps
        # the original "ClaudeUsageTrendTracker" name even though the app is Claude Usage Trend Tracker.
        PRODUCT_BUNDLE_IDENTIFIER: com.theportlandcompany.ClaudeMeter
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Sources/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: Sources/ClaudeUsageTrendTracker.entitlements
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: VP38993WK6
        ENABLE_HARDENED_RUNTIME: YES
        ENABLE_APP_SANDBOX: NO
      configs:
        Debug:
          CODE_SIGN_IDENTITY: "Apple Development"
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: "DEBUG"
        Release:
          # Defaults to Apple Development (the only cert on this Mac today).
          # Override at the command line for a public build, ex:
          #   xcodebuild CODE_SIGN_IDENTITY="Developer ID Application" ...
          CODE_SIGN_IDENTITY: "Apple Development"
        Release-AppStore:
          # TestFlight / Mac App Store: sandboxed, Apple Distribution, manual
          # profile minted via the ASC API (scripts/build-testflight.sh).
          CODE_SIGN_STYLE: Manual
          CODE_SIGN_IDENTITY: "Apple Distribution"
          CODE_SIGN_ENTITLEMENTS: Sources/ClaudeUsageTrendTracker-AppStore.entitlements
          ENABLE_APP_SANDBOX: YES
          PROVISIONING_PROFILE_SPECIFIER: "Claude Usage Trend Tracker Mac App Store"

  ClaudeUsageTrendTrackerTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests
        excludes:
          - "Fixtures"
      - path: Tests/Fixtures
        buildPhase: resources
    dependencies:
      - target: ClaudeUsageTrendTracker
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.theportlandcompany.ClaudeMeter.tests
        GENERATE_INFOPLIST_FILE: YES
        # Must match the host app's team, or the hardened-runtime host refuses
        # to dlopen the xctest bundle ("different Team IDs").
        CODE_SIGN_STYLE: Automatic
        DEVELOPMENT_TEAM: VP38993WK6
        # The app's PRODUCT_NAME has a space, so xcodegen's derived TEST_HOST
        # (which uses the target name) points at a bundle that does not exist.
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Claude Usage Trend Tracker.app/Contents/MacOS/Claude Usage Trend Tracker"
        BUNDLE_LOADER: "$(TEST_HOST)"

schemes:
  ClaudeUsageTrendTracker:
    build:
      targets:
        ClaudeUsageTrendTracker: all
        ClaudeUsageTrendTrackerTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - ClaudeUsageTrendTrackerTests
