# Gitleaks config for tpc-claude-usage-trend-tracker
[extend]
useDefault = true

[allowlist]
description = "Claude Code public OAuth client_id (PKCE native client — not a secret; ships in every client, used only to refresh user-added accounts)"
regexes = ['''9d1c250a-e61b-44d9-88ed-5944d1962f5e''']
